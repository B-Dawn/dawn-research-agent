# 研究方向推荐

生成时间：2026-09-06 14:39

每周可投入：约 12 小时

## 候选方向评分表

| 等级 | 方向 | 复合技能 | 复用度 | 热度 | 数据可获得性 | 预计工时 | 综合分 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| C | graph neural network AI security | graph neural network × AI security | 0.00 | 0.00 | 0.00 | 100 h | -14.3 |
| C | graph neural network low-altitude economy security | graph neural network × low-altitude economy security | 0.00 | 0.00 | 0.00 | 100 h | -14.3 |
| C | graph neural network UAV ad hoc network security | graph neural network × UAV ad hoc network security | 0.00 | 0.00 | 0.00 | 100 h | -14.3 |
| C | graph neural network network intrusion detection | graph neural network × network intrusion detection | 0.00 | 0.00 | 0.00 | 100 h | -14.3 |
| C | graph neural network graph neural network | graph neural network × graph neural network | 0.00 | 0.00 | 0.00 | 100 h | -14.3 |

## 证据（每个方向的代表性近期论文）

**C** —— graph neural network AI security

- 待补（离线模式未检索）

**C** —— graph neural network low-altitude economy security

- 待补（离线模式未检索）

**C** —— graph neural network UAV ad hoc network security

- 待补（离线模式未检索）

**C** —— graph neural network network intrusion detection

- 待补（离线模式未检索）

**C** —— graph neural network graph neural network

- 待补（离线模式未检索）

## 方向 -> 首个可行动作

### C · graph neural network AI security

- 第一周：用 `ra_search.py "graph neural network AI security"` 跑 20 篇，建对比矩阵
- 第二周：确认数据集是否拿得到，拿不到就换方向
- 第三周：写出一条可证伪的 claim，跑最小实验
- **终止条件**：连续两周拿不到数据、或最强竞品已覆盖该 claim，立即止损换方向

### C · graph neural network low-altitude economy security

- 第一周：用 `ra_search.py "graph neural network low-altitude economy security"` 跑 20 篇，建对比矩阵
- 第二周：确认数据集是否拿得到，拿不到就换方向
- 第三周：写出一条可证伪的 claim，跑最小实验
- **终止条件**：连续两周拿不到数据、或最强竞品已覆盖该 claim，立即止损换方向

### C · graph neural network UAV ad hoc network security

- 第一周：用 `ra_search.py "graph neural network UAV ad hoc network security"` 跑 20 篇，建对比矩阵
- 第二周：确认数据集是否拿得到，拿不到就换方向
- 第三周：写出一条可证伪的 claim，跑最小实验
- **终止条件**：连续两周拿不到数据、或最强竞品已覆盖该 claim，立即止损换方向

### C · graph neural network network intrusion detection

- 第一周：用 `ra_search.py "graph neural network network intrusion detection"` 跑 20 篇，建对比矩阵
- 第二周：确认数据集是否拿得到，拿不到就换方向
- 第三周：写出一条可证伪的 claim，跑最小实验
- **终止条件**：连续两周拿不到数据、或最强竞品已覆盖该 claim，立即止损换方向

### C · graph neural network graph neural network

- 第一周：用 `ra_search.py "graph neural network graph neural network"` 跑 20 篇，建对比矩阵
- 第二周：确认数据集是否拿得到，拿不到就换方向
- 第三周：写出一条可证伪的 claim，跑最小实验
- **终止条件**：连续两周拿不到数据、或最强竞品已覆盖该 claim，立即止损换方向

## 打分口径

`综合分 = 复用度×35% + 热度×25% + 数据可获得性×20% − 时间成本×20%`

- **复用度**：该方向的近期论文里，出现了你多少项其它技能——复用越多越省事
- **热度**：近 2 年论文数，太多说明红海，太少可能没社区
- **数据可获得性**：摘要中出现公开数据集/开源代码的密度
- **时间成本**：按复用度反推的预计工时，每周 12 小时下折算
